how to have a distance between image and profile text?
Navbar issue again
background issue in Doc Form rendering
change doctor name based on selection from which card/doctor the form has been clicked..........

handle clickoutside for both forms



		/*

		//Payment using AXIOS, currently INTERNAL SERVER ERROR (code:500)

		axios({
			method: 'POST',
			url: 'http://localhost:8080/process-payment',
			headers: {
			  'Accept': 'application/json',
			  'Content-Type': 'application/json',
			},
			//withCredentials: true,
			data: JSON.stringify(paymentObj)
		})
		.then((response) => {
				if (response.ok) 
				{
					return response.json()
				}
			})
		.then(({url}) => {
			console.log(url)
				//window.location = url
			})
		.catch((err) => console.log(err)) 

		*/
